

  <div>
  <h3>Valor</h3>
  <div>
    <input type="text" name="valor" value="{{pedido.valor}}">
  </div>
</div>

<div>
  <h3>Usuário</h3>
  <select name="vip">
    {% for vip in vips %}
      <option value="{{vip.id}}" {% if pedido.vip_id == vip.id %} selected="selected" {% endif %}>
        {{vip.apelido}}
      </option>
    {% endfor %}
  </select>
</div>


<div>
  <h3>Comprovante</h3>
  <div class="upload_gallery">
    <div class="photos_upload">
      <input class="photos_input" type="file" multiple="multiple" name="images" accept="image/*" onchange="PhotosUpload.handleFileInput(event)">
      <i class="material-icons">add</i>
    </div>
    <div class="photos_preview">
      {% if pedido.images %}
        {% for image in pedido.images %}
          <div class="photo" id="{{image.pedido_id}}">
            <img src="{{image.src}}" alt="{{image.name}}">
            <i class="material-icons" onclick="PhotosUpload.removeOldImage(event)">close</i>
          </div>
        {% endfor %}
        <input type="hidden" name="removed_files">
      {% endif %}
    </div>
  </div>

